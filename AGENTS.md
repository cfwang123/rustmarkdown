1. 总是用中文回复。
2. 乱码处理
	- 用`python -X utf8`运行python脚本
	- 用`chcp 65001; ...`运行powershell
	- 读取文件发现乱码时，不要修改这个文件
3. 只修改当前工作目录的文件。工作目录外的文件，若我在对话中提及，可直接只读访问，或临时复制到当前工作目录的`tmp/`下分析，无需再次询问；未提及的文件在查看前需征得同意。修改工作目录外文件仍需我的明确同意。
4. 工作目录下`tmp/`下放临时生成的脚本等。一次性的文件用完就删除。
5. PowerShell 转义问题
	- **不要**在 PowerShell 里直接塞复杂 Python/正则/引号（如 `text=\"...\"`、嵌套引号、`r'...'` 与双引号混用），极易触发 `ParserError` / 缺终止符。
	- **做法**：把逻辑写成 `tmp/` 下的临时脚本（`.py` / `.ps1`），再用命令调用
	- 参数用命令行 `sys.argv` 传入，避免在一行里拼大段代码。
	- 用完删除 `tmp/` 里一次性脚本与中间产物。
6. 写.gitignore, README.md时，隐藏本机路径、密钥、密码、Api Key
7. 修改大或复杂问题时，记录到 mydocs/TODO.md，并随修改过程更新。设计相关文件同样放在 mydocs/ 下；mydocs/ 已记入 .gitignore，不提交。mydocs/调试记录.md 只在解决了难题、踩坑时才记录，常规改动不写。
8. 修改的代码可能会破坏现有功能时，将重要代码文件备份到tmp/back/下，以便出问题时的回退、对照。
	- 用脚本对项目代码进行大规模替换、编辑时，把所有代码文件打包为7z，放到tmp/back/下备份。非代码文件可以不备份。
9. 同一个问题反复修改还不行时，用命令行/adb+日志自己调试
	- .net程序，增加命令行运行参数，通过命令行运行。
	- 安卓程序，通过adb+日志，模拟运行
	- 模拟问题场景，观察日志，发现问题根源
10. 代码风格指导（按需读取，动手改代码前先对照）
	- 编写/修改 **C#**（`.cs`、csproj/sln/slx 相关）时，先读并遵循 `D:/VS_Projects/AIPrototype/PROMPTS/csharp风格指导.md`（命名、组织、TickCount、WebSocket 服务器模式、编译方式等）。
	- 编写/修改 **JavaScript**（`.js` 等前端/模块脚本）时，先读并遵循 `D:/VS_Projects/AIPrototype/PROMPTS/JS代码风格指导.md`（IIFE 模块、私有函数、子模块、构造函数与方法语法等）。
	- 编写/修改 **C**（`.c` / `.h` 等）时，先读并遵循 `D:/VS_Projects/AIPrototype/PROMPTS/C语言风格指导.md`（签名/语句不折行、`{` 贴行尾、`}` 独占行、禁用 typedef、禁止 enum 作变量类型改用 uint、单语句 if/else 同行无括号等）。
	- 仅阅读、分析、解释已有代码而不改动时，可不强制加载；一旦要新增或改写实现，必须先加载对应风格指导再动手。
11. 修改功能时，同时更新当前工作目录下的 `README.md` 与 `CHANGELOG.md`。**改动记在 `CHANGELOG.md` 文首 `## unreleased` 下**（没有该章节则新建）；不要写进当前已发布版本章节。正式发版时再把 `unreleased` 改成「版本号 + 日期」，如 `## v1.2.3 (2026-06-01 ~ 03)`（跨多日写起止；单日写 `## v1.2.3 (2026-06-01)`）。无版本号的项目用日期 `## YYYY-MM-DD`。
12. 用es命令行搜索磁盘文件，注意乱码处理。
13. 用本机代理:7897访问非国内网站或下载资源
14. 每次改完代码后直接 `cargo build --release`（总是 Release，不要 debug）。`build.rs` 会在编译前自动结束正在运行的 `rustmarkdown.exe`。
15. **不要自己增加程序版本号**（`VERSION_DEFINE`、显示板/语音板、配置软件等）。版本号等我明确说再改；不要为一次改动新建更高版本号。
