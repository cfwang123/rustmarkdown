# Changelog

## v1.0.1 (2026-08-28)

- 源码任务框 `[ ]` 淡灰底、`[x]` 淡绿底，不再跟列表符一样无底色。
- 源码编辑区等宽字体改为优先 Consolas（接近 GVIM）；标题行与粗体也走同一等宽粗体，不再用雅黑 Bold。
- 预览中英混排垂直对齐：按汉字/拉丁脚本拆成多个 Label 并按行高底对齐；拉丁用独立 Ubuntu 副本下移基线，汉字走雅黑。源码 Consolas 同样下移基线对齐雅黑；源码行距为字号 × 1.45。
- 源码拖选：查找/预览映射底色也走 Galley 缓存；折行宽按 8px 量化，按住鼠标时宽度微变不整篇重排；侧边预览选区高亮在松开后再更新。
- 代码块折叠底栏省略号改为 `...`（原先 `⋯` 当前字体缺字形，显示成空心方块）。
- 已有实例时再打开文件（资源管理器 / 命令行）转到该窗口用新标签打开，并前置窗口；不新开第二个进程。
- 再次打开同一文件时恢复上次的视图模式与位置（Markdown 源行 / PDF·Word 页）；关标签后仍记住，写入 `session.json`（滚动时防抖保存）。
- 源码拖选卡顿：复用已排版的 Galley、缓存围栏区间、选区 hint 改为字节切片（不再从头 `chars().skip`）。
- 图片文件只读预览（对齐 docview ImageViewer）：打开 png/jpg/gif/bmp/ico/tif/webp；打开时按窗口居中适应；滚轮缩放、拖拽平移、双击适合/100%、`[` `]` 旋转；Ctrl+C / 右键复制；另存为 png/jpg/bmp。
- 侧栏目录树：点击选中整项（不再拖选文件名文字），鼠标为默认箭头。
- 会话记住目录树工作区根目录，下次打开自动恢复（命令行传入文件夹则用参数）。
- Windows 下 `cargo build` / `cargo run` 编译前自动结束正在运行的 `rustmarkdown.exe`，避免 exe 被占用导致链接失败。

## 0.1.0 (2026-08-26 ~ 28)

- M1 骨架：eframe/egui 窗口，`wins: Vec<Win>` 建模（当前仅主窗口）。
- 多标签：打开/关闭/切换/路径去重、拖动排序、中键关闭、未保存询问、Ctrl+Shift+T 重开。
- 三模式壳：代码、侧边预览（可拖分隔条）、预览；Ctrl+1/2/3 与 Ctrl+E。
- 打开/保存/另存为；拖放文件打开；命令行传入路径。
- 预览为纯文本占位（Markdown 原生渲染在后续版本）。
- M2：移植 MdParser（块/行内/表格/details/img、源行映射）；预览原生渲染（标题编号、列表、任务勾选、引用、代码 syntect、表格列宽、图片、链接）；编辑 180ms 防抖重解析；`--selftest` 对照 docview 用例。
- 设置程序图标（窗口/任务栏 + Windows exe 资源 `assets/icon.ico`）。
- Windows 下以 GUI 子系统编译，双击 exe 不再弹出控制台窗口。
- 支持打开 `.lnk`（文件或文件夹快捷方式）；拖放/打开文件夹时加载该目录顶层 Markdown/文本；Ctrl+Shift+O 选择文件夹。
- 修复 `.lnk` 目标含中文时按 UTF-8 误解码，状态栏显示方块并报「路径不存在」。
- 参数设置（工具栏 / Ctrl+,）：Markdown Tab 宽度（2/3/4/8）、标题自动编号、图片最大显示宽度；写入 `%LocalAppData%\rustmarkdown\settings.json`。
- 预览图片双击弹层（对齐 docview：适合区域、滚轮缩放、拖拽、Esc/点背景关闭）；右键「复制图片」「复制为文件」（资源管理器可粘贴）。
- 预览/编辑/标签栏关闭拖拽平移（pan），仅滚轮与滚动条滚动。
- 预览表格改为连续网格（表头底、共用边框、单元格顶对齐），不再是各自圆角小框。
- 围栏代码高亮覆盖常见语言（two-face 扩展语法 + `cs`/`csharp`/`js`/`ts`/`py`/`yml`/`sh`/`ps1` 等别名；`text`/`txt`/`log` 不着色；过大块纯文本）。
- 预览支持 ` ```mermaid ` 图表（纯 Rust 渲染，无浏览器；flowchart / sequence / class / state / pie 等常见类型；失败时显示源码）。
- 顶部菜单栏（文件 / 查看 / 工具 / 帮助）：打开文件夹、关闭全部、复制路径、在资源管理器中显示、关于；工具栏按钮改为线标图标，悬停显示功能名与快捷键。
- 修复引用块左侧竖线按剩余可视高度绘制，贯穿后续标题/代码块的问题。
- 预览/编辑滚动条贴窗口右边缘（不再被内容区内边距挤开）；滚轮速度加快。
- 滚动条深浅色都固定展开宽度（浮动不占内容宽，不再悬停变窄）。
- 源码编辑区按窗格宽度 break-word 折行，长行不再出现横向滚动条；超长无空格片段在行尾拆开以填满当前行。
- 代码编辑区背景改为纯白 `#ffffff`。
- 预览真正加粗：加载雅黑 Bold；`**粗体**` / `***粗斜体***` / 标题走粗体字族（egui `strong()` 原先只加深颜色）。
- 源码编辑区标题 / `**粗体**` / `***粗斜体***` 同样加粗（等宽粗体，避免光标错位）。
- 预览可按标题折叠（▶/▼，收起到下一同级或更高级标题）；围栏代码超过 10 行默认折叠，可「显示全部」。
- 修复侧边预览因分栏高度未约束而不显示滚动条。
- 行内 `<font color/style>` 支持前景、背景、bold、italic。
- Markdown 源码语法高亮（对齐 docview vim 风：标题分层色、标记灰、行内 code/链接/图、围栏 syntect）。
- 按下鼠标拖选文字时滚轮仍可滚动（编辑区 / 预览）。
- 图片弹层：去掉顶部黑条与标题，只保留右上关闭按钮（半透明底，避免压在图上看不清）。
- 预览任务列表 checkbox 只读显示，不可点击切换。
- 无参数启动恢复上次关闭时打开的文件与视图模式（`%LocalAppData%\rustmarkdown\session.json`）；命令行带路径时不恢复、按参数打开。
- 左侧 TOC 大纲侧栏（对齐 docview 章节列表 / mdview TOC）：F4 开关、筛选高亮、按级缩进折叠、滚动同步高亮当前节、点击跳转预览/源码；宽度与开关写入 `settings.json`。
- TOC 条目不可选中文字、指针为默认箭头；筛选后点击标题跳转并立即清空筛选。
- 源码标题行用雅黑 Bold（`#` 与标题正文同一字型），中英混排垂直居中；正文 `**粗体**` 仍用等宽粗体。等宽粗体优先配 Cascadia Mono Bold。
- 侧边预览双向同步滚动：仅用户滚轮/滚动条触发；程序化滚动 650ms 内只忽略对侧回传，本侧继续滚仍跟（对齐 docview）。
- 侧边预览当前光标块左侧 3px 蓝条；编辑区有选区时状态栏显示「已选择 N 字」。
- 侧边预览选区互相对应：源码选中→预览只给对应文字淡蓝底（不铺整块/整段）；预览拖选→源码对应行淡蓝底。
- 修复 Ctrl+Z 撤销一串输入后视口跳到很远位置：egui Undoer 把旧光标和正文绑在一起，撤销会还原更早的光标并 `scroll_to_rect`；现改为把光标钉在文本改动处。
- 修复预览表格右对齐时文字顺序颠倒：不再用 `Layout::right_to_left`（会把拆开的汉字倒排成「体斜」）；改为 LTR 排版，用左侧留白把整块顶到右边。
- 窗口标题栏显示版本号（`Cargo.toml` 的 `version`，如 `rustmarkdown v0.1.0`）。
- 后退 / 前进：大纲点击、预览 `#锚点`、文内相对 `.md` 链接记入窗口级历史（上限 50）；工具栏按钮、查看菜单、Alt+← / Alt+→；无历史时按钮禁用。同文件 `#标题` 与 `file.md#标题` 按 GFM 近似 slug 跳转。
- DOC / DOCX 只读预览（对齐 docview）：`office_oxide` 转为 Markdown 后按 A4 竖向分页（灰底白页、页间距 12）；大纲可跳转；不可覆盖原文件，另存为可导出 `.md`。解析在 16MB 栈线程、共享读，避免 Word 占用文件或复杂文档撑爆 UI 栈。
- PDF 只读预览（对齐 docview 连续页）：pdfium 按页光栅化并抽字；竖向排列；可见页懒渲染；页上拖选文字、Ctrl+C / 右键「复制文字」；大纲为「第 N 页」；双击弹层、右键复制图片。加密 PDF 会提示失败。
- 修复拖入 `.docx` 打不开：拖放接受 `path` 或 `bytes`、规范化 `file://` 路径、失败用错误对话框而不是只改状态栏。
- `node pack.js` 编译 Release 并打包到 `release/rustmarkdown_x.x.x.7z`（包内 exe / pdfium.dll / README.md / CHANGELOG.md；`release/` 已 gitignore）。
- 状态栏不再显示当前文件完整路径；打开 / 切换 / 保存 / 打开文件夹的提示只显示文件名或文件夹名。
- 修复表格等处 `**[文字](url)**` 只当粗体、链接未渲染的问题；粗体/斜体/高亮/删除线内的链接、代码会展开解析。
- 表格列宽改按 mdview 显示列单位分配：短列（Plugin / 链接名）钉死内容宽，长列才压缩；无空白的短列不拆成一字一折。
- PDF 选区改为 Sumatra 式亮黄半透明高亮；Ctrl+C / 右键「复制文字」走窗口剪贴板（修复复制无效）。
- PDF 打开时默认 100% 实际大小（1 点 = 1/72 英寸，按 96 DPI）；宽于窗口时出现横向滚动，不再铺满窗宽。
- Ctrl+F 查找（不区分大小写）：命中计数、F3 / Shift+F3 上下一个；源码黄底、预览按查询字高亮。
- 打开文本自动识别 UTF-8 / UTF-8 BOM / UTF-16 / GBK 等，状态栏显示编码，保存按原编码回写。
- 监视已打开文件：外部修改时自动重载；若有未保存修改则询问。
- 打开文件夹改为工作区目录树（侧栏「资源管理器 / 大纲」）；单击 md/Word/PDF 打开，右键复制路径 / 在资源管理器中显示。
- 标签右键「移到新窗口」或拖出标签栏松手：撕出独立窗口；副窗口标签清空后自动关闭。
- 标签拖拽对齐 docview：拖动过程标签跟手；离开标签栏立刻变成独立窗口并继续跟手（不必等松手）；拖到其它窗口标签栏立即合并；仅 1 个标签时不拆窗。
- 文件菜单「历史文件」：记录最近 20 个打开/保存的文件（去重、新的在前），写入 settings.json；不存在的项打开时会剔除。
- 文件夹目录树子文件夹默认全部收起。
- PDF / Word：Ctrl+滚轮、Ctrl++ / Ctrl+- 缩放，Ctrl+0 恢复 100%；状态栏显示百分比。
- 修复 Word 分页预览标题画出窗口顶栏（菜单/工具栏）的问题；长标签名裁切，不再盖住相邻标签。
- Word 页面在预览区水平居中（与 PDF 一致）；页外不再漏出竖排正文。
- 预览 PgUp/PgDn 翻页、方向键滚动（对齐 docview：PDF/Word 对齐到页顶；Markdown 预览按一屏；编辑框有焦点时不抢键）。
